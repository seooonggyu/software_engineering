<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.software_engineering.mapper.PermissionUserMapper">

	<!---->
	<select id="detail" parameterType="hashMap" resultType="com.project.software_engineering.dto.PermissionUserDto$DetailResDto">
		SELECT permission_user.id
			 , permission_user.deleted
			 , permission_user.created_at as createdAt
			 , permission_user.modified_at as modifiedAt

			 , permission_user.permission_id as permissionId
			 , permission_user.user_id as userId

			 , user.username as userUsername
		FROM permission_user, user
		WHERE permission_user.id = #{id}
		  AND permission_user.user_id = user.id
	</select>

	<!-- 공통 WHERE 절 -->
	<sql id="commonWhere">
		WHERE permission_user.id is not NULL
		<if test = "deleted != null">AND permission_user.deleted = #{deleted}</if>
		<if test="sdate != null and sdate != ''">AND permission_user.created_at >= CONCAT(#{sdate}, ' 00:00:00')</if>
		<if test="fdate != null and fdate != ''">AND CONCAT(#{fdate}, ' 23:59:59') >= permission_user.created_at</if>
		<if test = "permissionId != null and permissionId != ''">AND permission_user.permission_id = #{permissionId}</if>
		<if test = "userId != null and userId != ''">AND permission_user.user_id = #{userId}</if>
	</sql>

	<select id="list" parameterType="hashMap" resultType="com.project.software_engineering.dto.PermissionUserDto$DetailResDto">
		SELECT permission_user.id
		FROM permission_user
		<include refid="commonWhere"/>
	</select>

	<select id="pagedListCount" parameterType="hashMap" resultType="Integer">
		SELECT count(permission_user.id) as listsize
		FROM permission_user
		<include refid="commonWhere"/>
	</select>
	<select id="pagedList" parameterType="hashMap" resultType="com.project.software_engineering.dto.PermissionUserDto$DetailResDto">
		SELECT permission_user.id
		FROM permission_user
		<include refid="commonWhere"/>

		<if test = "orderby != null and orderby =='id'">ORDER BY permission_user.id </if>
		<if test = "orderby != null and orderby =='title'">ORDER BY permission_user.title </if>
		<if test = "orderway != null and orderway =='DESC'"> DESC</if>
		<if test = "orderway != null and orderway =='ASC'"> ASC</if>
		LIMIT #{offset}, #{perpage}
	</select>
	<select id="scrollList" parameterType="hashMap" resultType="com.project.software_engineering.dto.PermissionUserDto$DetailResDto">
		SELECT permission_user.id
		FROM permission_user
		<include refid="commonWhere"/>

		<if test = "orderby != null and orderby =='id'">
			<if test = "orderway != null and orderway =='DESC'">
				<if test = "mark != null and mark !=''">AND #{mark} > permission_user.id </if>
				ORDER BY permission_user.id DESC
			</if>
			<if test = "orderway != null and orderway =='ASC'">
				<if test = "mark != null and mark !=''">AND permission_user.id > #{mark}</if>
				ORDER BY permission_user.id ASC
			</if>
		</if>
		<if test = "orderby != null and orderby =='title'">
			<if test = "orderway != null and orderway =='DESC'">
				<if test = "mark != null and mark !=''">AND #{mark} > CONCAT(permission_user.title, '_', permission_user.id) </if>
				ORDER BY permission_user.title DESC
			</if>
			<if test = "orderway != null and orderway =='ASC'">
				<if test = "mark != null and mark !=''">AND CONCAT(permission_user.title, '_', permission_user.id) > #{mark}</if>
				ORDER BY permission_user.title ASC
			</if>
		</if>

		LIMIT 0, #{perpage}
	</select>
</mapper>