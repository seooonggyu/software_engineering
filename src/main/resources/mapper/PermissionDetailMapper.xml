<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.software_engineering.mapper.PermissionDetailMapper">

	<select id="access" parameterType="hashMap" resultType="com.project.software_engineering.dto.PermissionDetailDto$DetailResDto">
		SELECT
		permission_detail.id
		,permission_detail.target
		,permission_detail.func
		FROM permission, permissionUser, permission_detail
		WHERE permission.id = permissionUser.permission_id
		AND permission.id = permission_detail.permission_id
		AND permissionUser.user_id = #{userId}
		<if test = "target != null and target !=''">AND permission_detail.target = #{target}</if>
		AND permission_detail.func = #{func}
		AND permissionUser.deleted = false
		AND permission_detail.deleted = false
		AND permission.deleted = false
	</select>
	<!---->
	<select id="detail" parameterType="hashMap" resultType="com.project.software_engineering.dto.PermissionDetailDto$DetailResDto">
		SELECT permission_detail.id
			 , permission_detail.deleted
			 , permission_detail.created_at as createdAt
			 , permission_detail.modified_at as modifiedAt

			 , permission_detail.permission_id as permissionId
			 , permission_detail.target
			 , permission_detail.func
		FROM permission_detail
		WHERE permission_detail.id = #{id}
	</select>

	<!-- 공통 WHERE 절 -->
	<sql id="commonWhere">
		WHERE permission_detail.id is not NULL
		<if test = "deleted != null">AND permission_detail.deleted = #{deleted}</if>
		<if test="sdate != null and sdate != ''">AND permission_detail.created_at >= CONCAT(#{sdate}, ' 00:00:00')</if>
		<if test="fdate != null and fdate != ''">AND CONCAT(#{fdate}, ' 23:59:59') >= permission_detail.created_at</if>
		<if test = "permissionId != null and permissionId != ''">AND permission_detail.permission_id = #{permissionId}</if>
		<if test = "target != null and target != ''">AND permission_detail.target = #{target}</if>
		<if test = "func != null and func != ''">AND permission_detail.func = #{func}</if>
	</sql>

	<select id="list" parameterType="hashMap" resultType="com.project.software_engineering.dto.PermissionDetailDto$DetailResDto">
		SELECT permission_detail.id
		FROM permission_detail
		<include refid="commonWhere"/>
	</select>

	<select id="pagedListCount" parameterType="hashMap" resultType="Integer">
		SELECT count(permission_detail.id) as listsize
		FROM permission_detail
		<include refid="commonWhere"/>
	</select>
	<select id="pagedList" parameterType="hashMap" resultType="com.project.software_engineering.dto.PermissionDetailDto$DetailResDto">
		SELECT permission_detail.id
		FROM permission_detail
		<include refid="commonWhere"/>

		<if test = "orderby != null and orderby =='id'">ORDER BY permission_detail.id </if>
		<if test = "orderby != null and orderby =='title'">ORDER BY permission_detail.title </if>
		<if test = "orderway != null and orderway =='DESC'"> DESC</if>
		<if test = "orderway != null and orderway =='ASC'"> ASC</if>
		LIMIT #{offset}, #{perpage}
	</select>
	<select id="scrollList" parameterType="hashMap" resultType="com.project.software_engineering.dto.PermissionDetailDto$DetailResDto">
		SELECT permission_detail.id
		FROM permission_detail
		<include refid="commonWhere"/>

		<if test = "orderby != null and orderby =='id'">
			<if test = "orderway != null and orderway =='DESC'">
				<if test = "mark != null and mark !=''">AND #{mark} > permission_detail.id </if>
				ORDER BY permission_detail.id DESC
			</if>
			<if test = "orderway != null and orderway =='ASC'">
				<if test = "mark != null and mark !=''">AND permission_detail.id > #{mark}</if>
				ORDER BY permission_detail.id ASC
			</if>
		</if>
		<if test = "orderby != null and orderby =='title'">
			<if test = "orderway != null and orderway =='DESC'">
				<if test = "mark != null and mark !=''">AND #{mark} > CONCAT(permission_detail.title, '_', permission_detail.id) </if>
				ORDER BY permission_detail.title DESC
			</if>
			<if test = "orderway != null and orderway =='ASC'">
				<if test = "mark != null and mark !=''">AND CONCAT(permission_detail.title, '_', permission_detail.id) > #{mark}</if>
				ORDER BY permission_detail.title ASC
			</if>
		</if>

		LIMIT 0, #{perpage}
	</select>
</mapper>